importe std.io como u;
importe std.conversions como c;
importe std.texto como s;

int[3] numbers := [1,2,3];

numbers[1] := 10;

u.diga_nl(numbers[1]);

importe utils como uuu;

u.diga_nl(uuu.mmm);

u.diga(-verdadeiro);
