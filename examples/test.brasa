importe std.io como u;
importe std.conversions como c;
importe std.texto como s;

int age := c.int(u.leia());

u.diga_nl(age);
u.diga(age >= 0);

texto name := "Ellea\\\'\"\tnor";

u.diga(s.maiusculo(name));
